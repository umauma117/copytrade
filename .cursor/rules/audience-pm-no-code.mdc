---
description: 对话对象背景——产品经理、代码零基础；解释方式与沟通偏好
alwaysApply: true
---

# 用户背景（请始终考虑）

- 用户是 **产品经理**，**没有代码基础**，不要假设对方懂终端、Git、环境变量、防火墙等术语。
- **解释方式**：
  - 用 **中文**、**口语化**，像给同事讲步骤，少用英文缩写；若必须用（如 RPC、SSH），用一句话说明「是什么、能干什么」。
  - 给 **具体操作**（点哪里、复制哪一行、先后顺序），而不是只讲原理。
  - 安全/部署类内容：用 **「要做 / 不要做」清单**，每条一行，避免抽象概括（例如不要只说「收紧权限」，要说「不要把私钥文件放进微信或网盘」）。
- **代码修改**：尽量 **少改、可回滚**，改完用一两句话说明「改了什么、对你有什么影响」。
